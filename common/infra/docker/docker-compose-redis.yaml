version: "3.9"

services:
  redis-main:
    image: redis:7.4.1
    container_name: redis-main
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
