#
# Handle line endings automatically for files detected as text 
# and leave binary files untouched.
* text=auto

# Explicitly declare text files you want to always be LF 
# (Highly recommended for shell scripts and YAMLs)
*.sh eol=lf
*.yml eol=lf
*.yaml eol=lf