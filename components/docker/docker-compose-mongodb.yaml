version: "3.9"

services:
  mongo-main:
    image: mongo:7.0
    container_name: mongo-main
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
